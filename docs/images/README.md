# docs images

Screenshots referenced by [`../DEBUGGER.md`](../DEBUGGER.md). Drop the PNGs here with these exact names:

- **`combo-session.png`** — the `claude` session driving the debugger step by step (the bullet trace of tool calls).
- **`combo-trace.png`** — the round-by-round runtime trace table Claude built (round / points / combo / total / should-be).
- **`combo-fix.png`** — the "The bug" / "The fix" write-up.

(Once added, you can delete this README or leave it.)
